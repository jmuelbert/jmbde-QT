#if !defined(CONFIG_APPY_H)
#define CONFIG_APPY_H

#cmakedefine APPY_INSTALL_QT_LAYOUT
#define APPY_DEBUG_POSTFIX "${CMAKE_DEBUG_POSTFIX}"

// relative install dirs
#define APPY_PLUGIN_INSTALL_DIR "${PLUGIN_INSTALL_DIR}"
#define APPY_LIBEXEC_INSTALL_DIR "${LIBEXEC_INSTALL_DIR}"
#define APPY_BUNDLE_INSTALL_DIR "${BUNDLE_INSTALL_DIR_PRIVATE}"
#define APPY_BIN_INSTALL_DIR "${BIN_INSTALL_DIR}"
#define APPY_PROBE_INSTALL_DIR "${PROBE_INSTALL_DIR}"
#define APPY_TRANSLATION_INSTALL_DIR "${TRANSLATION_INSTALL_DIR}"
#define APPY_QCH_INSTALL_DIR "${QCH_INSTALL_DIR}"
#define APPY_TARGET_PLUGIN_INSTALL_DIR "${TARGET_PLUGIN_INSTALL_DIR}"

#define APPY_PLUGIN_VERSION "${APPY_PLUGIN_VERSION}"
#define APPY_PROBE_ABI "${APPY_PROBE_ABI}${APPY_PROBE_ABI_POSTFIX}"

// relative inverse install dirs
#define APPY_INVERSE_BUNDLE_DIR "${APPY_INVERSE_BUNDLE_DIR}"
#define APPY_INVERSE_BIN_DIR "${APPY_INVERSE_BIN_DIR}"
#define APPY_INVERSE_LIB_DIR "${APPY_INVERSE_LIB_DIR}"
#define APPY_INVERSE_PROBE_DIR "${APPY_INVERSE_PROBE_DIR}"
#define APPY_INVERSE_LIBEXEC_DIR "${APPY_INVERSE_LIBEXEC_DIR}"

// probe name
#define APP_PROBE_BASENAME "${PROBE_BASENAME}"

// default network properties
#define APPY_DEFAULT_LOCAL_ADDRESS "127.0.0.1"
#define APPY_DEFAULT_LOCAL_TCP_URL "tcp://127.0.0.1"
#define APPY_DEFAULT_ANY_ADDRESS "0.0.0.0"
#define APPY_DEFAULT_ANY_TCP_URL "tcp://0.0.0.0"

// build options
#cmakedefine HAVE_STDINT_H
#cmakedefine HAVE_BACKTRACE
#cmakedefine HAVE_CXA_DEMANGLE

#cmakedefine HAVE_QT_WIDGETS
#cmakedefine HAVE_QT_SCXML
#cmakedefine HAVE_QT_SVG
#cmakedefine HAVE_QT_DESIGNER
#cmakedefine HAVE_QT_PRINTSUPPORT

#cmakedefine HAVE_SYNTAX_HIGHLIGHTING

#cmakedefine HAVE_ELF_H
#cmakedefine HAVE_SYS_ELF_H
#cmakedefine HAVE_ELF

#cmakedefine APPY_ENABLE_GPL_ONLY_FEATURES
#cmakedefine APPY_CORE_ONLY_LAUNCHER
#cmakedefine APPY_STATIC_PROBE
#cmakedefine APPY_DISABLE_FEEDBACK

#cmakedefine APPY_WITH_WIDGET3D

#define QT_LIBINFIX "@QT_LIBINFIX@"

#endif // CONFIG_APPY_H